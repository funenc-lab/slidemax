# Repository Skills

This directory stores repository-local skills.

- `ppt_master_workflow/` packages the PPT Master workflow surface, including roles, commands, workflows, and references.

When adding new workflow-facing assets, prefer placing them under the relevant skill directory instead of adding more root-level command files.
