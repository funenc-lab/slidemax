# Image Plan

This curated example does not depend on external raster artwork during validation.

- Use shape-only layouts and system fonts for the current reference output.
- If future revisions require imagery, keep the generated assets inside `images/` and document their prompts or source metadata here.
