# PPT Master Python dependencies
#
# Install the shared bundle:
#   pip install -r requirements.txt
#
# Most commands use the Python standard library. Install the packages below
# according to the workflow you need.

# PPTX export and Office-compatible SVG fallback
python-pptx>=0.6.21
svglib>=1.5.0
reportlab>=4.0.0

# Source ingestion
PyMuPDF>=1.23.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Image processing and local diagnostics
Pillow>=9.0.0
numpy>=1.20.0

# AI image providers
# Gemini provider
google-genai>=1.0.0

# OpenAI-compatible provider
openai>=1.0.0

# Doubao / ARK provider
volcengine-python-sdk[ark]>=1.0.0
